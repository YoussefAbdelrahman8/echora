# face_recognition.py — STUB (real implementation coming later)
def detect_face(frame):
    """Returns face confidence 0.0-1.0. Returns 0 if no face detected."""
    return 0.0

def identify_face(frame):
    """Returns (name, details) tuple if face identified. Returns ('', '')."""
    return "", ""